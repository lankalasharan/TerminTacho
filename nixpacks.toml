[phases.install]
cmds = ["npm install --include=dev --no-audit --fund=false"]

[phases.build]
cmds = ["npx prisma generate", "NODE_OPTIONS=--max-old-space-size=1536 NEXT_TELEMETRY_DISABLED=1 npm run build"]

[start]
cmd = "npm run start"
